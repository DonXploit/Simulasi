/** @type {import('next').NextConfig} */
const nextConfig = {
  // Opsi konfigurasi khusus Next.js dapat ditambahkan di sini
  // Misalnya: output: 'export' untuk static HTML export (jika dibutuhkan)
  // images: { unoptimized: true } (jika menggunakan output: 'export')
};

export default nextConfig;
